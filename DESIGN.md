# SkillDock 界面规范

用户于 2026-09-07 选择 D「原生工作间」，补充要求：风格简约，蓝白为主。拨盘：视觉冒险度 5、动效强度 3、信息密度 7。

## 核心原则

白色内容区、浅灰导航、蓝色操作与选中态。每页只出现一个主标题，不常驻展示「正常」「已加载」等系统自述。用排版与间距区分层次，减少重复容器边框、来源胶囊和装饰图标。保留当前桌面端、CLI 与 Rust 能力。

首屏应看见 Skill 名称、来源、分发目标。首次使用从统一目录设置进入扫描；归集和分发仍先预览再执行，冲突与恢复入口不能隐藏。预设保留整体分发和取消，取消时仍说明手动或其他预设的引用会保留。

## 视觉参数

- 画布/面板：`#ffffff`；导航：`#f7f9fc`；分隔线：`#e9edf3`。
- 文字：`#202936`；次要：`#657184`；蓝：`#2463d4`；选中背景：`#edf4ff`。
- 中文系统字体；英文品牌优先 Avenir Next，路径用 SFMono-Regular。中文无斜体。
- 主标题 28px/600，正文与操作 14px，次要信息 12–13px；数字等宽。
- 导航 204px，内容边距 36px；控件圆角 6px，弹层 12px。
- 默认蓝白；已有深色设置保留，使用中性色板与同一蓝色语义。

参考 DNA：采用 Notion 的轻边框思想（黑色约 10%）、8px 基础间距、克制的 4/8/12px 圆角与低透明度分层阴影；采用 Warp 的平面表面层级和少量重音策略。没有引入营销页的巨型标题或渐变。

## 组件与行为

- 导航：按资料库/工作空间分组，整块选中底色，不用左侧强调竖线；窄窗收为图标导航并保留可访问名称。
- Skill 表格：用于多列比较和批量选择。名称是键盘可访问的详情入口；主列只留名称、来源、目标、待处理更新。版本与完整来源放详情。选中行整块淡蓝，底部固定批量操作栏；筛选为空时保留筛选并提供清除。
- 次级筛选：来源、目标、预设、更新状态置于筛选区域；顶部始终保留搜索、结果数量和全部/已分发切换。
- 预设：整行列举名称、成员与目标数，详情、整体分发、取消分发均可访问。无成员时提供编辑入口，不画空白卡片墙。
- 设置：平面分组与轻量导航，保留标签和解释，长路径可换行。
- 弹层：沿用 Reka 焦点管理、Esc 和焦点返还；出现动画 ≤180ms，降低动态偏好关闭位移。
- 通知：成功提示改为短时浮层，不推动页面；错误原位显示并可关闭；恢复任务有明确入口。

## 工艺细节

品牌几何线标、蓝色文本选区、蓝色 focus-visible、输入框 focus-within、整行选中底色、细分隔线、数字等宽、低强度分层弹窗阴影、克制滚动条。高频导航无入场动画。

## 验证范围

两轮实际渲染检查：Skill 库、首次归集、预设、发现、目标、更新、任务、设置及弹层。验证搜索/筛选/选中/详情/预设操作和窄窗、深色兼容。前端类型检查、生产构建、macOS 桌面包构建分别记录；不因此重复迁移用户真实目录。

### 下拉选择

全部下拉选择使用统一的 `AppSelect`（Reka UI Select），禁止使用可见的原生 select。触发器高度 38px，菜单沿用蓝白与深色主题令牌，选中项显示勾选标记；支持键盘导航、Escape 关闭、长列表滚动与弹窗中的浮层定位。空字符串筛选值与数字更新间隔保持原始类型。

### 状态栏弹窗打磨（2026-09-08）

功能契约：3 秒内识别收录数量、更新状态并找到工作台入口；保留检查更新、配置定时更新、退出及键盘操作。沿用 336×412 原生窗口与跨屏定位，使用命令列表而非嵌套卡片。

继承 Notion 参考的 8px 间距基准、12px 弹层圆角、1px 轻边框与 2px 焦点环；使用现有蓝色和中文系统字体。统计采用等宽数字、单行摘要，主命令淡蓝强调，其余命令保持平面。状态灯静止，错误可换行滚动并提供恢复入口。高频菜单开关不做位移、缩放或延迟，按钮只提供短时颜色反馈；不改变主窗口激活与屏幕定位策略。

状态栏任务概览：顶部并列显示中央库 Skill 数量与已启用来源更新策略的任务数量（notify/auto，每来源一个任务）。暂停／开启位于任务状态旁；零任务时禁用并引导配置。暂停不会中断已启动的一轮更新，也不改写来源策略；当前调度暂停状态随应用运行周期生效。


## 归集、备份和恢复完整性

- 扫描排除与内容复制分离。发现列表可跳过依赖、构建目录和系统 Skill；实体归集必须完整复制所选目录实际依赖的包内容，校验一致后才替换为软链。新导入超过容量或文件数上限时整体失败，不截断内容。下载 Git 的视图排除克隆管理元数据 `.git`，本地目录的 `.git` 保留。
- 一次归集提交为一个文件事务。备份记录原目录的完整清单摘要，链接按链接文本比较，不跟随；因此包内跨 Skill 的相对资源引用可还原。旧备份按兼容规则恢复，额外依赖若未进入旧统一库副本则禁止自动清理。旧备份恢复采用流式校验，不受新导入大小限制。
- 保留次数按归集批次计算；同一分钟分组仅用于展示。超额备份在持久化文件清单后逐项清理；中断后允许已删除条目缺失，新增或变化内容停止该批次，其他超额批次继续处理。任务和备份列表提供清理重试入口。
- 恢复允许原位置仍是匹配的受管软链，也允许取消分发后的空闲位置。原位置被其他内容占用时拒绝覆盖。空闲路径恢复失败时回滚为原来的空闲状态，不创建多余软链。
- 恢复仅移除该次归集新增且没有其他分发或预设引用的 Skill 记录；仍使用的来源保留，并在原安装已恢复且无其他归集链接时重新允许更新。用户当前的更新策略保留。
- 恢复中断依靠幂等文件回滚重试；先持久化恢复后的状态，再终结日志。兼容旧日志已 rolledBack、任务仍 needsRecovery 的中断状态。
- 统一库实体永久清理尚未接入生产恢复流程，等待用户单独确认。旧归集备份的保留清理与此区分。

### Skill 库按名称展示与工具切换（2026-09-09）

- 列表以统一库为准，同名 Skill 只占一行；标题、侧栏、分页和选择数量按合并后的行数计算。保留底层来源记录、快照和现有分发链接，名称相同不代表内容完全相同。
- 每行显示全部已配置分发目标。未分发显示加号，已分发显示勾选；点击直接执行分发或取消手动分发。新增分发优先选用统一库快照，按安装时间、ID 确定稳定代表；现有工具绑定的其他副本不切换版本。工具名与路径用于区分同一工具的多个目录。
- 分发前自动调用计划接口检查冲突，并携带当前 revision 执行。冲突、过期或外部内容占用时展示错误，不静默覆盖。请求中禁用切换；失败后刷新实际状态。
- 多选行后可批量分发或取消，弹层选择一个或多个工具并确认范围。分发逐目标提交，仅补齐缺少的绑定；部分完成时明确显示数量，重试自动跳过已完成项。取消汇总同名所有副本在所选工具上的手动绑定，保留预设引用与借用链接。
- 仅有预设引用的工具按钮显示锁和“预设”，需从预设页整体取消。加入预设只加入每行代表条目；预设已有同名成员时保持原成员，避免同名路径冲突。打开目录汇总同名成员的链接，打开原目录指向代表条目的实体。
- 回归：`node scripts/test-library.mjs` 覆盖分组、绑定匹配、批量补齐、预设保护、部分失败重试及重复点击。

### Finder 元数据与快照恢复（2026-09-09）

实际分发曾因来源包快照校验失败被拒绝，包内 `.DS_Store` 由 Finder 改写会参与旧摘要。保持旧版完整摘要校验；经“重新收录当前内容”生成的新快照使用忽略普通 `.DS_Store` 文件的内容摘要，其他文件、权限和软链继续校验。迁移、导入导出、回滚和实体清理识别两种摘要；备份清单继续完整校验。

重新收录先预览 revision、内容摘要和关联 Skill 数量，再确认创建新快照、更新该包当前 Skill 记录；原快照、预设锁与现有绑定保持。提交时重新检查 revision/内容摘要，拒绝预览后的文件变化及成员缺失。分发失败在列表原位展示，并提供重新收录入口；批量失败按错误中的快照摘要定位需要处理的包。

### 未配置更新来源的交互（2026-09-09）

- 已归集并脱离原位置的来源显示“待配置来源”，Skill 库及更新中心直接提供“配置更新来源”；批量检查仅执行可检查来源并提示跳过数量。本地引用显示“跟随本地内容”。
- 配置支持 Git 仓库（分支、子目录）或独立源码目录，先预览匹配成员，再确认保存。当前以来源包为单位，必须匹配全部现有成员的名称和相对路径，不将来自不同仓库的集合误绑定到单个仓库。
- 禁止将统一库或工具分发目录用作本地更新源。保存时重验内容摘要和 revision；只修改来源元数据，保留 Skill、分发绑定和预设，定时策略默认关闭。
- 验证：57 项 Rust 测试、8 项前端测试及前端生产构建通过，CLI/桌面调试构建通过；浏览器使用隔离数据验证来源状态与列表直达弹窗。真实来源地址未改写，Git 远程拉取未做真实联网验收。

### Skill 目录显示名称（2026-09-10）

工具目标在 Skill 库、筛选器、分发/预设选择与计划、详情和软链目录选择中统一展示所属工具。优先按工具标识匹配配置；旧的自定义目标按已配置或默认工具路径识别。`skills` 显示为 `Codex · Skills` 等名称；已有工具前缀不重复添加，自定义名称保留并补充工具。共享 `.agents/skills` 无明确工具标识时显示通用 Agent Skills，无法识别的目录使用父目录区分，不凭末级 `skills` 猜测工具。

仅处理显示，不改保存的名称、目录路径、目标 ID 或分发记录。回归入口：`node scripts/test-library.mjs`；类型与构建：`npm run build`。

更新中心的来源名称使用同一工具路径识别规则，覆盖来源列表、待处理更新、详情标题及检查结果提示，Skill 库的来源筛选和详情同步复用。本地 folder/local/local_reference 来源按目录识别所属工具；Git 和站点来源保留原名称，避免将远程仓库名 `skills` 误标为本地工具。补充来源回归后共 10 项测试通过，类型检查与生产构建通过；未进行原生窗口点击验证，未提交或发布。

### 来源与计划仅列出已配置项（2026-09-10）

归集产生的 detached 来源记录不默认进入“来源与计划”表格，分页总数以已配置来源为准；没有已配置来源时显示空状态。保留底层来源及 Skill 关联，通过“配置更新来源”入口按需选择：单项直接配置，多项弹窗选择，核对并保存成功后进入主列表。从 Skill 库传入 sourceId 的配置入口继续有效，检查失败但已配置的来源继续展示。

验证：10 项既有回归、类型检查和生产构建通过；浏览器演示确认已配置来源及分页正常展示。本轮未完成原生未配置来源的配置保存验收，未提交、推送或发布。
