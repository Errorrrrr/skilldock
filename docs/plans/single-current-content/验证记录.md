# 单份当前内容验证记录

验证日期：2026-09-22；发布版本：0.3.0。

## 范围

在普通临时目录导出精确暂存文件后验证，不使用 worktree。原工作区已有的宽泛运行时文件忽略规则及配套用例未纳入提交，也未用于此次发布验证。未改变用户真实 Skill 库或工具软链。

## 自动检查

- Rust 工作区 98 项现有测试通过，覆盖核心、CLI 集成与桌面端。旧固定版本/历史清理测试显式构造旧格式，继续保障迁移前行为。
- 前端 10 项现有回归、类型检查、生产构建通过。
- 发布脚本 4 项测试、四处版本一致性、生成 DTO 一致性、Rust/前端格式和差异空白检查通过。

## 隔离文件场景

- 连续三次来源更新：一份当前内容、一份最近恢复备份，两个工具及名称入口同步；撤销后回到一份，预设不保留内容锁。
- 同名相同完整包重复导入：复用记录与对象，返回有效的现存 ID，记录收录来源。对已经自动加后缀的独立内容重复导入也复用。
- 不同内容明确替换：保留原 Skill 身份和名称、合并管理工具及预设；撤销恢复原内容和原来源。
- 来源包包含共享依赖和新增成员：撤销恢复旧依赖，撤销新增成员的分发和引用。
- 旧库同名冲突未选择则拒绝；确认后所有目标统一。仅接管与旧记录完全匹配的名称及 name@version 链接，整理为公开名称目录与隐藏管理目录。
- 目标链接被外部替换时更新拒绝，其他工具和外部内容均不被覆盖。
- 删除目标期间连续更新：实际旧链接仍保护旧对象；重新添加通过历史绑定身份及完整内容校验自动绑定并切换当前内容。

## 界面与边界

浏览器演示确认：详情展示名称目录、当前内容和收录来源；更新页不显示固定/历史版本入口；替换弹窗未选择时禁用提交；明确替换后出现撤销入口，恢复后入口消失且内容说明复原。浏览器演示不作为原生文件操作证据，文件场景使用独立 CLI 核心验证。

本次未进行原生安装、旧版应用安装升级及 Windows/Linux 真机验收。真实旧库需要升级后自行预览并确认迁移；被修改、被实际外部引用或归集原目录备份仍需要的对象不会强行清理。

发布仅执行主分支合入和版本标签触发。流水线最终构建、签名与公开发布结果按用户要求不持续监听。
